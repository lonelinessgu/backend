# Небольшие изменения

### Основное:
- Поменял прямое обращение к бд на относительное как к Model/RoleModel, идея состоит в том что бы исправить нелогичность которая возникает при изменение названия базы, где хранятся пользователи, ввиду которого происходит казус, что надо перелопатить половину auth просто меняя названия, что является не очень прикольной задачей.</br>
- Так же изменил [main.py](backend/main.py) для лучшей работы, если нет env == error, hardcode path to dotenv file, так же логирование в [users_roles](backend/models/users_roles.py) для проверки ролей, что бы в случае fuckup ситуации было понятно, что не так.</br>
- Ну и моя продолжающаяся война с обосанным passlib и щедевро поддержкой bcrypt v 0.0.0.0 в итоге откатил bcrypt до 4.0.1
